# Sentry Node Configuration
# Sentry nodes protect validators from direct public exposure

# Network selection
NETWORK=testnet

# Docker images
RETH_IMAGE=ghcr.io/orqus-chain/orqus-reth:latest
ORQUSBFT_IMAGE=ghcr.io/orqus-chain/orqusbft:latest
COMETBFT_IMAGE=cometbft/cometbft:v0.38.15

# Data directory
DATA_DIR=./data

# IMPORTANT: Your public IP address
EXTERNAL_IP=

# RPC binding (127.0.0.1 = local only, 0.0.0.0 = public)
RPC_HOST=127.0.0.1
RPC_PORT=8545
WS_PORT=8546

# ============================================
# VALIDATOR CONNECTION (Internal Network)
# ============================================
# These should be internal/VPN IPs, NOT public IPs

# CometBFT Validator Peers (get from K8s cluster)
# Format: node_id@internal_ip:26656,node_id@internal_ip:26656
# Example: abc123@10.0.1.10:26656,def456@10.0.1.11:26656
VALIDATOR_COMETBFT_PEERS=

# Reth Validator Enodes (get from K8s cluster)
# Format: enode://pubkey@internal_ip:30303,enode://pubkey@internal_ip:30303
VALIDATOR_ENODES=

# Validator Node IDs (to hide from external nodes)
# Comma-separated list of CometBFT node IDs
VALIDATOR_PRIVATE_IDS=

# ============================================
# How to get Validator info from K8s:
#
# # CometBFT node ID
# kubectl exec -n orqus-test orqus-node-test-0 -c cometbft -- cometbft show-node-id
#
# # CometBFT internal IP (K8s service)
# orqus-node-test-0.orqus-node-test-headless.orqus-test.svc.cluster.local:26656
#
# # Or use VPN IP if sentry is outside K8s VPC
# ============================================
