version: '3.8'

# Test Network Override
# Usage: docker compose -f docker-compose.yml -f docker-compose.test.yml up -d

services:
  cometbft:
    volumes:
      - ${DATA_DIR:-./data}/cometbft:/cometbft
      - ../../networks/test/genesis.json:/cometbft/config/genesis.json:ro
      - ./configs/test/cometbft.toml:/cometbft/config/config.toml:ro
